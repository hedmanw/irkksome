<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/server_connect_legacy"
        android:background="@drawable/states_split_list"
        android:paddingTop="16dp"
        android:paddingLeft="8dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@android:id/text1" />

        <TextView
            style="@style/list_item.hint"
            android:text="(2 hours ago) --> ikonen ska eg. vara ett X"
            android:layout_below="@android:id/text1"
            android:id="@android:id/text2" />
    </RelativeLayout>
    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="70dp"
        android:paddingRight="16dp"
        android:paddingLeft="16dp"
        android:src="@drawable/send_now"
        android:background="@drawable/states_split_list"
        android:layout_alignParentRight="true"
        android:id="@+id/server_connect_legacy_remove"/>
    <View
        android:layout_width="1dp"
        android:layout_height="54dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:background="@android:color/darker_gray"
        android:layout_toLeftOf="@id/server_connect_legacy_remove"/>
</RelativeLayout>